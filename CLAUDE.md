# 작업 방침

- 코드 수정이 끝나면 사용자에게 확인받지 않고 바로 `git add` → `commit` → `push origin main`까지 진행한다. GitHub main에 push되면 Vercel이 자동으로 프로덕션에 배포한다.
- 단, 아래의 경우는 예외로 먼저 사용자에게 확인한다: 큰 리팩터링/파일 삭제처럼 되돌리기 번거로운 변경, lint/build 에러가 있는 상태, 또는 사용자가 명시적으로 "아직 push하지 마"라고 한 경우.
